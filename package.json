{
  "name": "foodshare-deploy",
  "version": "1.0.0",
  "scripts": {
    "install": "cd backend && npm install && npm rebuild sqlite3 --build-from-source && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": "20.x"
  }
}
